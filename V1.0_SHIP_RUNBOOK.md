# KitchenSink4PPT v1.0 — Ship Runbook

Written 2026-08-30 (pre-ship, expansion wave in flight). For the session that
ships. Follow in order; every step has a verify. Author involvement is
needed ONLY at the two steps marked [AUTHOR].

## 0. Preconditions (all must be true)

- [ ] Full suite green (`.venv\Scripts\python.exe -X utf8 -m pytest tests/ -q`,
      timeout >= 420s; the stdio tests make the suite ~2 min).
- [ ] COM validator pass on the artifact set with the author's PowerPoint
      closed; zombie check clean.
- [ ] Adversarial + discoverability gates green (both already passed at tag
      `v1.0-gate-green`; re-run regressions only if code changed since).
- [ ] Source-original checksums still OK:
      `md5sum -c research/corpus_source_checksums.txt`.
- [ ] Working tree committed; expansion waves integrated.

## 1. Version + counts stamp

- [ ] Set version `1.0.0` in ALL THREE places (Word lesson: one file has two
      fields): `pyproject.toml`, `server.json`, `bundle/manifest.json`
      (BOTH version fields if the bundle exists; if no bundle ships in v1.0,
      note that in the release body and skip).
- [ ] Measure real counts from the live server, never by hand:
      tool count via a script that imports the server and lists tools
      (lite + full), test count from the pytest summary, ops count if
      marketed. Record them; they feed step 2.

## 2. Public-copy fill + sweeps (docs/ + README)

- [ ] Replace `{{TOOL_COUNT}}`, `{{TEST_COUNT}}`, `{{OPS_COUNT}}` in
      docs/index.html (ship-day comment block at the top lists every
      location), docs/llms.txt, docs/storefront/index.html. Verify zero
      `{{` remain: `grep -r "{{" docs/ README.md`.
- [ ] Confirm the storefront NAME matches the author's final ruling
      (pending at time of writing: leaning "The KitchenSink Works" with
      subline "Everything, plus the kitchen sink."). Do not ship the
      storefront with an unapproved name.
- [ ] Sweeps, all must return zero hits in public files (README, docs/,
      docstrings ship-checked by tests already):
      em/en dashes; dissertation vocabulary (`M+`, `Delta Model`, `MDT`,
      `Goals`.`Tasks`.`Bonds` as a cluster, `mediated alliance`); personal
      info (author real-world identifiers, military, Korea/Seoul,
      university names).
- [ ] llms.txt pack listings match the shipped registry (tool names).

## 3. GitHub

- [ ] `gh repo create nometalalchemist/KitchenSink4PPT --public` (gh is
      keyring-authed). Push main + tags.
- [ ] Verify Actions run green (tests.yml matrix uses the synthetic corpus;
      real corpus is gitignored).
- [ ] Enable Pages: Settings > Pages > Deploy from branch > main, /docs.
      Verify https://nometalalchemist.github.io/KitchenSink4PPT/ renders,
      drag demo works, llms.txt reachable.
- [ ] Issue templates / Service Department forms (copy KS4W's if not
      already in repo).

## 4. PyPI [AUTHOR]

- [ ] Author registers the Trusted Publisher on PyPI for
      `kitchensink4ppt`: repo nometalalchemist/KitchenSink4PPT, workflow
      `publish.yml`, environment `pypi` (mirrors KS4W setup; ~2 minutes,
      needs their PyPI login).
- [ ] `gh release create v1.0.0` with release notes (headline: native
      tweakable graphics, the drag-the-node story; honest maturity section).
      CI publishes to PyPI via OIDC; verify `pip index versions
      kitchensink4ppt` (allow a few minutes).

## 5. MCP Registry [AUTHOR]

- [ ] Confirm `server.json` name `io.github.nometalalchemist/kitchensink4ppt`
      and the PyPI mcp-name marker in the README/package match.
- [ ] Author runs the device-code login for mcp-publisher, and the publish
      fires IMMEDIATELY after auth (the JWT dies in under an hour,
      confirmed twice on KS4W). `mcp-publisher publish`.
- [ ] Verify the registry entry lists v1.0.0.

## 6. Family storefront + KS4W strip (order matters: links 404 otherwise)

- [ ] Create repo `nometalalchemist.github.io` (user root site), move
      docs/storefront/index.html to its root as index.html, push, verify
      the root URL renders.
- [ ] Update the two staging storefront links in KS4P's docs/index.html to
      the root URL, delete docs/storefront/, redeploy Pages.
- [ ] Apply docs/ks4w_family_strip_patch.md to word-mcp's docs/index.html
      (single anchored edit), run its four-step verification, push KS4W.

## 7. Listings + announce (same day, best effort)

- [ ] Glama: claim/verify the new server (Dockerfile already present for
      introspection).
- [ ] awesome-mcp-servers PR (badge + bot requirements per KS4W's merged
      PR #13054 as the template).
- [ ] mcpservers.org submission.
- [ ] Outreach comments (respectful, working-solution style, per the
      preeminence plan): python-pptx #132 (slide duplication),
      mermaid-js/mermaid#3239 (editable diagram export), the GongRzhe
      archive's most-reacted template issues IF comments are open
      (the repo was comment-locked for KS4W's round; check).
- [ ] r/claudemcp post mirroring the KS4W launch post format.

## 8. Post-ship

- [ ] BUILD_LOG.md gets the ship entry (numbers, timeline, lessons).
- [ ] Memory checkpoint: project_mcp_tooling.md + MEMORY.md index line
      updated (KS4PPT SHIPPED + version + link).
- [ ] Field test begins: the defense deck. Bug reports via Developer
      Feedback/ folders; expect the Word-style day-three wave.
- [ ] KS4XL research fan-out is next week's session; the storefront shelf
      is waiting.

## Rollback

Tags: `v1.0-gate-green` (pre-expansion), `wave4-green`. Snapshots + git
bundle in `../Backups/` (20260830_2124). Website pre-go-wild copy:
`../Backups/docs_v1_pre_gowild_20260830_2103/`. PyPI cannot un-publish a
version: ship a .post1 if a release is bad. Registry entries can be
re-published over.
